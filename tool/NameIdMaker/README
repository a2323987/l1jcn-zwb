　文字列のうちdesc.tblに存在する部分を$xxxのnameid形式に置换するツールです。

■使い方

コマンドライン: java -jar NameIdMaker.jar filename [language]

filename: 置换对象文字列を列记したテキストファイルのパス
language: 元のテキストファイルの言语。省略するとj:日本语が指定されます


1. NameIdMaker.jar、desc-j.tbl、对象のテキストファイルの三つを同じフォルダに入れます。

2. java -jar NameIdMaker.jar name.txt j

3. 置换されたテキストがname_id.txtとして出力されます。


．置换后の文字列の前后に空白以外の文字が续く场合はクライアントで读み取れなくことがあるので置换されません。オリジナルアイテムの多くがこれに该当するでしょう。
　("～ケイブ3F" -> "～ケ$13103F" など)

．文字列ができるだけ短くなるように置换します
　"シルバー ロング ソード"の场合 "$656(シルバー) $0(ロング ソード)" と"$1147(シルバー ロング ソード)" では后者が选ばれる。
/////google/////
Desc.tbl部分字符串中存在变数$XXX的nameid形式，以取代格式。

■使用

命令行： Java -jar NameIdMaker.jar filename [language]

文件名：路径到一个文本文件，列出更换字符串对象
语言：语言的原始文字。如果您省略记者：被指定由日本


1. NameIdMaker.jar、desc-j.tbl、三到同一文件夹中的对象的文本。

2. java -jar NameIdMaker.jar name.txt j

3. Name_id.txt替换文字的输出。


．是或如果非空白字符之前或之后替换不是取代，因此，客户端可能会挂起读米。这将许多原来的该当项目。
（ “ ～洞窟3楼” - “ ” ～ $ 13103F案“ ，等等）

．将尽量简短，这样的字符串
“龙银之剑”的“ $ 656 （银） $ 0 （长剑） ”和“ $ 1147 （银色长剑） ”选择了后者。
